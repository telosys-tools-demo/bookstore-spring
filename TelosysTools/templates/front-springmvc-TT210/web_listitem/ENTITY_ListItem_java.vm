/*
 * Created on $today.date ( Time $today.time )
 * Generated by $generator.name ( version $generator.version )
 */
#set( $varName = $fn.uncapitalize($entity.name) )
package ${target.javaPackageFromFolder($SRC)};

import ${ENTITY_PKG}.${entity.name};
import ${ROOT_PKG}.web.common.ListItem;

public class ${entity.name}ListItem implements ListItem {

	private final String value ;
	private final String label ;
	
	public ${entity.name}ListItem(${entity.name} $varName) {
		super();

		this.value = ""
#foreach( $attribute in $entity.keyAttributes )
			#if( $foreach.count > 1 ) + "|" #end + ${varName}.${attribute.getter}()
#end
		;

		//TODO : Define here the attributes to be displayed as the label
		this.label = ${varName}.toString();
	}

	@Override
	public String getValue() {
		return value;
	}

	@Override
	public String getLabel() {
		return label;
	}

}
